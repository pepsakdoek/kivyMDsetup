BoxLayout:
    orientation: 'vertical'
    MDToolbar:
        right_action_items: [["dots-vertical",lambda the_button: app.dropdown.open(the_button)]]
        title: "MDDropdownMenu"
        md_bg_color: app.theme_cls.primary_color
